# Part 4: Logical Operators and IF Statements 

## 1. Find the Largest of Three Numbers

- Takes 3 integers as input.
- Uses **nested `if` statements** to compare and determine the largest.
- Prints the largest value.
